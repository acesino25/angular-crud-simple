export interface Cliente{
    Nombre ? : string,
    Apellido ? : string,
    Email ? : string,
    Dni ? : number,
    Nacimiento ? : Date,
    Telefono ? : string,
    Direccion ? : string,
    hide ? : boolean
}